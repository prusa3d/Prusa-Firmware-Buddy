/**
 * \addtogroup      ESP_UNICODE
 * \{
 *
 * Unicode manager is able to decode sequence of `UTF-8` characters.
 * Length of sequence can vary between `1` to `4` bytes.
 *
 * \note            Unicode manager is used to decode sequence of characters
 *                  when scanning list of access points
 *
 * \}
 */