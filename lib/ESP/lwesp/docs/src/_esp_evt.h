/**
 * \addtogroup      ESP_EVT
 * \{
 *
 * Event helper function consist of user friendly way to get data/information from events.
 *
 * Functions may only be called from `event callback` function, defined by user.
 *
 * \include         _example_evt.c
 *
 * \note			More functions will be added to cover as much events as possible
 *
 * \}
 */