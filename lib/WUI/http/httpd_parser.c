/*
 * httpd_parser.c
 *
 *  Created on: Aug 1, 2020
 *      Author: joshy
 */
#include "httpd_parser.h"

uint32_t httpd_json_parser(char *json, uint32_t len) {
    // sending commands through HTTPD post is disabled for now
    // will be implemented in the future if needed
    return 0;
}
